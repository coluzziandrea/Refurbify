name: AuthTests

on:
  push:
  pull_request:
    branches:
      - 'develop'
      - 'master'
      - 'release/*'
      - 'feature/*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v2
      - run: cd refurbify/auth && npm install && npm run test:ci
